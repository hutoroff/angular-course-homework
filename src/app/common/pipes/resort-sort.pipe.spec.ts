import { ResortSortPipe } from './resort-sort.pipe';

describe('ResortSortPipe', () => {
  it('create an instance', () => {
    const pipe = new ResortSortPipe();
    expect(pipe).toBeTruthy();
  });
});
